"use strict"    //treat entire js code as newer version

//alert(2+2)    //give error because we are using node instead browser


let userName="Ram"          //string
let userAge=18             //number
let userPresent=true     //boolean 
let userLocation        //undefined datatype
let cityTemp=null      //object datatype (it means not set/get due to error/issue any value bot defined)


//bigint is also one type of datatype that is used for very big number

console.table([typeof userName,typeof userAge, typeof userPresent, typeof userLocation, typeof cityTemp])
